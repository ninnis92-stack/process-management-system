## Release notes — 2026-03-06

Summary:

- Added admin-driven inbound email routing (`EmailRouting` model) and CRUD UI.
- Updated inbound webhook to consult routing mappings and SSO-owner fallback.
- Guarded optional `sentry-sdk` imports to avoid editor diagnostics and added lazy init.
- Moved the full README contents to `docs/README_FULL.md` and shortened top-level `README.md`.
- Added Alembic migration stub for `email_routing` table (`migrations/versions/0008_add_email_routing.py`).
- Tests: full pytest suite passes locally (18 passed) and Cypress smoke tests pass against deployed staging.

Deployment:

- Deployed to Fly app `process-management-prototype-lingering-bush-6175` on 2026-03-06.
- Health endpoint returned `{"status":"ok"}` and smoke records were cleaned.

Next steps:

- Apply Alembic migration on target DB: `flask db upgrade` or ensure `release_command` runs migrations.
- Add unit tests covering `EmailRouting` CRUD and webhook integration (recommended).
- Optionally create a GitHub release from tag `v0.1.0-2026-03-06` and attach these notes.
