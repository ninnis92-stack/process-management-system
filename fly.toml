# fly.toml app con***REMOVED***guration ***REMOVED***le generated for process-management-prototype
# See https://fly.io/docs/reference/con***REMOVED***guration/ for information about how to use this ***REMOVED***le.

app = "process-management-prototype"
primary_region = "ewr"

[build]

[env]
  PORT = "8080"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 1024
