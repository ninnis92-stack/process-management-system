{
  "name": "process-management-prototype-e2e",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run --browser chrome"
  },
  "devDependencies": {
    "cypress": "^12.10.0",
    "vitest": "^4.1.0"
  }
}
