replicaCount: 1

image:
  repository: ghcr.io/your-org/process-management-prototype
  pullPolicy: IfNotPresent
  tag: latest

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: false

resources: {}
