name: Deploy to Fly

on:
  push:
    branches: [ main ]

env:
  FLY_API_TOKEN_SECRET_NAME: FLY_API_TOKEN
  PRODUCTION_ADMIN_EMAIL_SECRET_NAME: PRODUCTION_ADMIN_EMAIL
  PRODUCTION_ADMIN_PASSWORD_SECRET_NAME: PRODUCTION_ADMIN_PASSWORD
  WEBHOOK_SHARED_SECRET_NAME: WEBHOOK_SHARED_SECRET

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      APP_URL: https://process-management-prototype-lingering-bush-6175.fly.dev
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Run tests
        run: |
          PYTHONPATH=. pytest -q
      - name: Install flyctl
        run: |
          curl -L https://fly.io/install.sh | sh
      - name: Deploy to Fly
        env:
          FLY_API_TOKEN: ${{ secrets[env.FLY_API_TOKEN_SECRET_NAME] }}
        run: |
          ~/.fly/bin/flyctl deploy -a process-management-prototype-lingering-bush-6175
      - name: Run post-deploy smoke checks
        env:
          SMOKE_ADMIN_EMAIL: ${{ secrets[env.PRODUCTION_ADMIN_EMAIL_SECRET_NAME] || 'admin@example.com' }}
          SMOKE_ADMIN_PASSWORD: ${{ secrets[env.PRODUCTION_ADMIN_PASSWORD_SECRET_NAME] || 'admin123' }}
          WEBHOOK_SHARED_SECRET: ${{ secrets[env.WEBHOOK_SHARED_SECRET_NAME] }}
        run: |
          PYTHONPATH=. bash scripts/smoke_test.sh "$APP_URL"
          PYTHONPATH=. python scripts/smoke_deployed_login.py --url "$APP_URL"
          PYTHONPATH=. python scripts/admin_smoke.py --url "$APP_URL"
          PYTHONPATH=. python scripts/webhook_smoke.py --url "$APP_URL" --secret "$WEBHOOK_SHARED_SECRET"
      - name: Clear smoke data
        if: always()
        env:
          SMOKE_ADMIN_EMAIL: ${{ secrets[env.PRODUCTION_ADMIN_EMAIL_SECRET_NAME] || 'admin@example.com' }}
          SMOKE_ADMIN_PASSWORD: ${{ secrets[env.PRODUCTION_ADMIN_PASSWORD_SECRET_NAME] || 'admin123' }}
        run: |
          PYTHONPATH=. python scripts/clear_smoke_remote.py --url "$APP_URL"
